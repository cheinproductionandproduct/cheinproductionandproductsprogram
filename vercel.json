{
  "buildCommand": "prisma generate && next build",
  "framework": "nextjs",
  "installCommand": "npm install",
  "crons": [
    {
      "path": "/api/cron/line-past-due",
      "schedule": "0 1 * * 5"
    }
  ]
}
